import React from 'react'
import { ReactComponent as Icon } from "../../app/icons/modelIcon.svg"
import './index.scss'

export default function ModelIcon() {
    return (
        <Icon className={`modelIcon `} />
    )
}
